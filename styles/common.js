import colors from './colors';

const buttonStyle = {
    flex: 1,
    width: 150,
    borderWidth: 1,
    alignItems: 'center',
    justifyContent: 'center',
    borderRadius: 5,
    borderColor: colors.white,
}

const common = {
    buttonStyle
}

export default common;