import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
    list: {
        flex: 1,
        width: '100%',
        alignContent: 'center',
        textAlign: 'center'
    },
});

export default styles;